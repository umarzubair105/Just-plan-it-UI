import { ShowErrorsDirective } from './show-errors.directive';

describe('ShowErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
